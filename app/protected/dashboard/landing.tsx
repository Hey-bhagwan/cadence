// app/dashboard/loading.tsx
import SkeletonLoader from '@/components/SkeletonLoader'; // We will create this next

export default function Loading() {
  // You can add any UI you want here, but a skeleton loader is common.
  return <SkeletonLoader />;
}